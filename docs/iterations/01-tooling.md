# 第一批：工程工具

## 产出

- 依赖迁移至 `pyproject.toml` 和 `uv.lock`，移除 `requirements.txt`。
- 增加 uv、Ruff、mypy、pytest、pytest-django 和 mdformat 配置。
- 增加 `make format`、`make lint`、`make test`、`make check`。
- 增加主页、注册和 Celery 任务测试。

## 验证

最终验收结果见[第四批记录](04-ci-and-release.md)。

## 遗留项

无。
