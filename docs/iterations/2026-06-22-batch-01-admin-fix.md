# 第 1 批验收修复：校园与标签后台

## 修复内容

- 将校园、标签和通知模块加入 SimpleUI 显式菜单。
- 校区、标签分类和标签列表支持搜索、筛选、分页和列表快速编辑。
- 保留单学校约束：系统已有学校时不再显示新增入口。
- 新增后台菜单和增删改查页面可用性测试。

## 验证

- 聚焦后台测试：7 项通过。
- `make format`：通过。
- `make lint`：通过，mypy 和 Django system check 无问题。
- `make test`：通过，43 项测试全部成功。
- `uv lock --check`：通过，解析 50 个包。
- 迁移漂移检查：通过，无变更。
