# 第 10 批（管理后台）：暖橙主题统一

## 实施内容

### 1. Admin 主题色统一

#### `static/admin/css/accounts-admin.css`
- `--admin-accent`：蓝色 `#3559e0` → 暖橙 `#FF8C42`
- `--admin-accent-soft`：浅蓝 `#eef3ff` → 浅橙 `#FFF0E0`
- 所有表单聚焦 ring、按钮、文件选择器按钮等颜色自动继承新变量

#### `static/admin/css/dashboard.css`
- `.dashboard-eyebrow`：蓝色 `#3559e0` → 暖橙 `#FF8C42`
- `.dashboard-primary`：蓝色背景/阴影 → 暖橙背景/阴影

#### `static/admin/js/dashboard.js`
- ECharts 折线图主色：蓝色 `#3559e0` → 暖橙 `#FF8C42`
- ECharts 折线图渐变：蓝色 rgba → 暖橙 rgba
- ECharts 饼图"正常"区块：绿色 `#19a974` → 暖橙 `#FF8C42`

### 2. 未改动的模板
- `templates/admin/dashboard.html` — 结构正确，无需改动
- `templates/admin/accounts/user/change_list.html` — 继承 admin 模板，按钮样式由 SimpleUI 控制
- `templates/admin/accounts/user/bulk_add.html` — 继承 `admin/base_site.html`，样式由 `accounts-admin.css` 控制

### 3. SimpleUI 配置
- 当前配置保留：`SIMPLEUI_CONFIG` 中 `system_keep=False`、`menu_display=["用户管理"]`、`dynamic=False`
- SimpleUI 主题色修改需要在其自身的配置界面或 CSS 中覆盖，本次通过自定义 admin CSS 覆盖关键颜色

## 验证结果
- 蓝色颜色检查：无 `#3559e0` 或 `#19a974` 残留

## 保留的业务逻辑
- Dashboard 数据加载（`user_dashboard_data`）
- ECharts 图表渲染
- 批量用户导入（CSV 校验 + 事务）
- 所有 admin 表单功能

## 待后续批次
- SimpleUI 原生主题色配置（可通过 SimpleUI 主题系统进一步统一）
