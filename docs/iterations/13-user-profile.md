# 第十三批：个人中心

## 完成内容

- 用户模型增加可选昵称和统一 `display_name` 展示属性。
- 展示身份按昵称、脱敏手机号、用户名回退，手机号注册用户不再暴露随机内部用户名。
- 新增登录保护的个人中心，可修改昵称、用户名并补充真实邮箱。
- 手机号作为已验证登录标识保持只读，避免无验证码换绑。
- 导航栏和首页欢迎信息统一展示昵称，并链接个人中心。
- Django Admin、migration 和 MySQL 8 参考 SQL 同步昵称字段。
- 新增响应式个人中心页面和业务级样式文件。

## 验证结果

- 定向认证与个人中心测试：23 项通过。
- 个人中心静态样式查找：通过。
- `make format`：通过。
- `make lint`：通过。
- `make test`：25 项通过。
- `uv lock --check`：通过，解析 47 个包。
- `make check`：通过，25 项测试通过。
- `uv run python manage.py makemigrations --check --dry-run`：通过，无变更。
